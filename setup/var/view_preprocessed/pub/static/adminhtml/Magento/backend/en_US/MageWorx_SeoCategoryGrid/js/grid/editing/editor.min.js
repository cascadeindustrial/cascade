define(['underscore','Magento_Ui/js/grid/editing/editor'],function(_,Editor){'use strict';return Editor.extend({defaults:{storeId:0},save:function(){var data;var storeId;if(!this.isValid()){return this;}
storeId=this.storeId?this.storeId:0;data={items:this.getData(),store_id:storeId};this.clearMessages().columns('showLoader');this.client().save(data).done(this.onDataSaved).fail(this.onSaveError);return this;}});});